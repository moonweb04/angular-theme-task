import { InputWarningDirective } from './input-warning.directive';

describe('InputWarningDirective', () => {
  it('should create an instance', () => {
    const directive = new InputWarningDirective();
    expect(directive).toBeTruthy();
  });
});
