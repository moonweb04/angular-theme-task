import { ViewchildDirective } from './viewchild.directive';

describe('ViewchildDirective', () => {
  it('should create an instance', () => {
    const directive = new ViewchildDirective();
    expect(directive).toBeTruthy();
  });
});
